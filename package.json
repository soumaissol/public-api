{
    "name": "delete-test-01",
    "description": "delete-test-01-description",
    "version": "0.0.1",
    "type": "module",
    "private": true,
    "dependencies": {
        "aws-sdk": "^2.799.0",
        "node-fetch": "^3.3.1",
        "yup": "^1.2.0"
    },
    "devDependencies": {
        "jest": "^26.6.3"
    },
    "scripts": {
        "test": "jest",
        "invoke:calculate": "sam local invoke -t template.yaml -e events/event-calculate.json -n env.json Calculate",
        "invoke:distributors": "sam local invoke -t template.yaml -e events/event-get-power-distributors.json -n env.json GetPowerDistributors"
    }
}
