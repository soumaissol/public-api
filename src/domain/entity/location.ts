// sa means state abreviation, equivalent to UF in Brazil

class Location {
  constructor(readonly addressName: string, readonly city: string, readonly state: string, readonly sa: string) {
  }
}

export default Location;
